version: 1
services:
  - type: web
    name: suhitha
    env: python
    region: oregon
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: daphne suhitha.asgi:application

